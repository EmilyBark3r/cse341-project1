// const router = require('express').Router();

// const getInstructorsController = require("../controllers/instructors");

// const {
//     isAuthenticated
// } = require("../middleware/authenticate");

// router.get("/", getInstructorsController.getAllInstructors);
// router.get("/:id", getInstructorsController.getInstructors);
// router.post("/", isAuthenticated, getInstructorsController.postInstructors);
// router.put("/:id", isAuthenticated, getInstructorsController.putInstructors);
// router.delete("/:id", isAuthenticated, getInstructorsController.deleteInstructors);


// module.exports = router;